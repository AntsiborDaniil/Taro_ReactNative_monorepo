# Локальный AI API (Fastify в apps/api, порт по умолчанию 3002).
# Для HttpOnly cookie не смешивайте localhost и 127.0.0.1: откройте сайт и API с одним и тем же hostname.
EXPO_PUBLIC_TAROT_API_BASE_URL=http://127.0.0.1:3002
