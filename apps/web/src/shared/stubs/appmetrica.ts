const AppMetrica = {
  activate: (_config?: unknown) => {},
  reportEvent: (_name: string, _params?: unknown) => {},
  reportError: (_name: string, _error?: unknown) => {},
  setUserProfileID: (_id: string) => {},
};

export default AppMetrica;
